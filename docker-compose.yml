version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NEXT_PUBLIC_API_URL=${API_URL:-http://localhost:3000}
    restart: unless-stopped
    networks:
      - megabuy

networks:
  megabuy:
    external: true
